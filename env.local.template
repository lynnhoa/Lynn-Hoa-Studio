# ─── Supabase ─────────────────────────────────────────────────
# Copy these from: Supabase Dashboard → Project Settings → API
#
VITE_SUPABASE_URL=https://YOUR_PROJECT_ID.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_ANON_PUBLIC_KEY

# ─── Notes ────────────────────────────────────────────────────
# VITE_ prefix is required — Vite only exposes vars with this prefix
# to the client bundle (import.meta.env.VITE_*).
# Never commit this file. It is already in .gitignore via Vite defaults.
